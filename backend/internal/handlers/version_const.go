package handlers

// BackendVersion is the current version of the backend.
const BackendVersion = "1.4.8"

// FrontendCompatibleVersion is the semver range of compatible frontend versions.
const FrontendCompatibleVersion = "^1.1"
